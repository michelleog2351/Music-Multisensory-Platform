@page "/dashboard"
@using MusicBased_IOT_Platform.Application.Services
@* @inject IJSRuntime JS
 *@
 @inject UserSessionService Session
@inject NavigationManager NavigationManager

<h3>My Dashboard</h3>

<PageTitle>My Dashboard</PageTitle>
<p>Session User: @Session.CurrentUser?.FirstName</p>

<br />
<select class="form-control">
    <option value="daily">Daily</option>
    <option value="weekly">Weekly</option>
    <option value="monthly">Monthly</option>
</select>


@* d3 barchart *@
<div>
    <canvas id="myChart" 
            width="400" 
            height="200"></canvas>
</div>



@code {

    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender && Session.CurrentUser != null)
        {
            NavigationManager.NavigateTo("/dashboard", true);
        }
    }

    // protected override async Task OnAfterRenderAsync(bool firstRender)
    // {
    //     if (firstRender)
    //     {
    //         var data = new[]
    //         {
    //             new { name = "FG", value = 12000 },

    //         };

    //         await JS.InvokeVoidAsync("renderBarChart", "chart", data);
    //     }
    // }
}
