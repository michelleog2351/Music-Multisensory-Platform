name: Docs

on:
  push:
    branches:
      - develop
      - main

jobs:
  docs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Install DocFX
        run: dotnet tool install -g docfx

      - name: Add dotnet tools to PATH
        run: echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

      - name: Debug repo structure
        run: |
          echo "ROOT:"
          ls
          echo "----"
          echo "MusicBased_IOT_Platform:"
          ls MusicBased_IOT_Platform || echo "folder not found"
          echo "---- SEARCH FOR DOCFX ----"
          find . -name "docfx.json"

      - name: Generate docs
        run: |
            dotnet tool install -g docfx
            echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
            docfx ./MusicBased_IOT_Platform/MusicBased_IOT_Platform/MusicBased_IOT_Platform/docfx.json
